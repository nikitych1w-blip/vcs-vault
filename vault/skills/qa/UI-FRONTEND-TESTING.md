# Скилл: Ручное тестирование фронтальных сценариев (UI/FE)

> Базовый скилл: `BASE-TESTING.md`  
> Артефакты SA: `vault/specs/`  
> Спецификации: `sources/vcs-playwright/`

---

## Зоны ответственности

1. **Анализ UI-узлов test-model** — изучение структуры экранов, фич, операций
2. **Верификация UX-требований** — сверка поведения интерфейса с AC в spec.md
3. **Написание TC для UI** — каждый TC привязан к узлу UI через `node` и `lv`
4. **Ручное тестирование интерфейсов** — клики, навигация, заполнение форм
5. **Проверка адаптивности** — desktop, tablet, mobile (если применимо)
6. **Тестирование состояний** — пустые состояния, загрузка, ошибки
7. **Дефекты UI** — баги с указанием экрана, элемента, версии API (web1/web2)

---

## Жизненный цикл UI тестирования

```
Фаза 1  Онбординг         →  Изучить vault/test-model/1. UI/, структуру экранов
Фаза 2  Анализ покрытия   →  Инвентаризация узлов lv6–lv8, выявление missing
Фаза 3  Согласование с SA  →  Проверка, что все AC покрываются UI-тестами
Фаза 4  Подготовка TC     →  Написание TC по формату для UI-узлов
Фаза 5  Ручное тестирование →  Открытие экранов, выполнение сценариев
Фаза 6  Отчётность        →  Дефекты, обновление статусов, метрики
```

---

## Структура UI test-model

### Уровни UI (всё читать от корня к листу)

```
UI / Обзор / Статистика / Коммиты / За сегодня
UI / Проект / Репозитории / Репозиторий / Код / Ветки / Создание / WEB
UI / Запросы на слияние / Запросы / Открыто / Фильтры / По автору
UI / Профиль и настройки / Аватар / Загрузка
```

**Ключевые моменты:**
- `lv6` — фича (Ветки, Коммиты, Теги)
- `lv7` — операция (Создание ветки, Удаление тега)
- `lv8` — атомарный сценарий (WEB, API, RESTRICTED — ограничения)

### Теги UI

| Тег   | Значение                    |
|-------|-----------------------------|
| `#UI` | Веб-интерфейс               |
| `#FE` | Frontend-специфика (web1/web2) |
| `#v2` | Версия web2 (Новый FE)      |

### Версии UI API

| Тег     | Значение                    |
|---------|-----------------------------|
| `web1`  | Frontend API web1           |
| `web2`  | Frontend API web2 (ПАО)     |

**Важно:** Поведение web1 и web2 может отличаться — тестировать отдельно.

---

## Анализ UI-требований

### Структура UI-требования (из SA)

```markdown
## Фича: Фильтрация MR по автору

**Узел**: UI / Запросы на слияние / Запросы / Открыто / Фильтры / По автору  
**Уровень**: lv8  
**Теги**: #UI #SC #merge #request  
**Версия API**: web2

### Предусловия
- Пользователь авторизован
- В репозитории есть MR от разных авторов

### Основной сценарий
1. Перейти в Запросы на слияние → Открыто
2. Нажать фильтр → По автору
3. Выбрать автора из выпадающего списка
4. Подтвердить фильтрацию

### Альтернативные сценарии
- Нет MR по фильтру → отображается заглушка
- Сброс фильтра → список возвращается в исходное состояние

### Логика реализации
- Фильтрация через web2 API
- Показать MR, где author.id = selected
- Кнопка "Сброс" удаляет фильтр из URL
```

### Проверка требований

**Чеклист:**
- [ ] Предусловия реализуемы в интерфейсе
- [ ] Шаги однозначны и воспроизводимы
- [ ] Ожидаемые результаты конкретны (не "работает корректно", а "отображается 1 MR")
- [ ] Альтернативные сценарии описаны
- [ ] Указаны edge cases (пустой список, невалидные данные)

---

## Формат TC для UI

```markdown
---
id: TC-XXXX
node: UI / Запросы на слияние / Запросы / Открыто / Фильтры / По автору
lv: 8
tags: [UI, SC, merge, request, FE]
api_version: web2
status: covered
priority: high
tms: TC-XXXX
---

## TC-XXXX: Фильтрация MR по автору

**Узел**: `UI / Запросы на слияние / Запросы / Открыто / Фильтры / По автору` (lv8)  
**Версия API**: web2  
**Теги**: `#UI` `#SC` `#merge` `#request`

### Предусловия
- Пользователь авторизован
- В репозитории есть хотя бы 3 MR от разных авторов (user1, user2, user3)

### Шаги
1. Открыть репозиторий
2. Перейти на вкладку «Запросы на слияние»
3. Открыть вкладку «Открыто»
4. Нажать кнопку «Фильтры»
5. В разделе «Автор» выбрать из выпадающего списка → `user2`
6. Нажать «Применить»

### Ожидаемый результат
- В списке отображается только 1 MR (от author=user2)
- Счётчик MR обновился на 1
- В URL добавлен параметр `author=user2`
- В списке фильтров отображается активный фильтр «Автор: user2»

### Альтернативные сценарии

#### Сценарий A: Нет MR по фильтру
**Шаги:** Выбрать автора, у которого нет MR
**Ожидаемый результат:**
- Отображается заглушка: «Нет запросов на слияние»
- Кнопка «Сброс» активна
- Счётчик MR = 0

#### Сценарий B: Сброс фильтра
**Шаги:** После применения фильтра нажать «Сброс»
**Ожидаемый результат:**
- Список возвращается к исходному состоянию (все MR)
- Параметр `author` удаляется из URL
- Активный фильтр исчезает из списка

### Проверки UI-элементов
- [ ] Кнопка «Фильтры» отображается
- [ ] Выпадающий список авторов содержит всех авторов MR
- [ ] Выбранный автор подсвечивается
- [ ] Кнопка «Применить» активна после выбора
- [ ] Кнопка «Сброс» активна при наличии фильтров
- [ ] Заглушка пустого списка имеет правильный дизайн
- [ ] URL обновляется корректно

### Состояния экрана
- [ ] Ручная прокрутка работает
- [ ] Пагинация работает (если > 20 MR)
- [ ] Изменение ширины окна → адаптивная верстка
- [ ] Анимации переходов плавные

### Фактический результат
- [ ] Шаг 1: OK
- [ ] Шаг 2: OK
- [ ] Шаг 3: OK
- [ ] ...

### Комментарий тестировщика
- ...

### Скриншоты
- ![Результат](path/to/screenshot.png)
```

---

## Чеклист ручного UI тестирования

### Подготовка
- [ ] Получен список UI-узлов для тестирования (lv6–lv8)
- [ ] Тест-кейсы написаны и утверждены
- [ ] Browser: Chrome / Firefox / Safari (версия указана)
- [ ] Пользовательские данные подготовлены
- [ ] Актуальные данные для тестирования (MR, ветки, коммиты)

### Выполнение
Для каждого TC:
- [ ] Предусловия выполнены
- [ ] Шаги выполнены по порядку
- [ ] Ожидаемый результат проверен для каждого шага
- [ ] Альтернативные сценарии протестированы
- [ ] UI-элементы проверены (visibility, clickable, text)
- [ ] URL обновляется корректно
- [ ] Анимации не вызывают багов
- [ ] Адаптивность (если применимо)

### Проверки состояния
- [ ] Лоадеры исчезают после загрузки
- [ ] Ошибки отображаются корректно (toast/modal)
- [ ] Состояния загрузки обновляются
- [ ] Кэширование не мешает (если нужно обновление)

### Фиксация результата
- [ ] При успехе: обновлён `status` TC в TMS
- [ ] При дефекте: создан BUG-XXXX с:
  - Узлом test-model
  - Версией UI (web1/web2)
  - Скриншотом / видео
  - Консольными ошибками (если есть)
- [ ] Связанный TC указан в баге

---

## Дефект-репорт для UI

```markdown
## BUG-XXXX: Фильтрация MR не обновляет URL

**Узел**: UI / Запросы на слияние / Запросы / Открыто / Фильтры / По автору  
**Уровень**: lv8  
**Теги**: `#UI` `#SC` `#merge` `#request`  
**API версия**: web2  
**Severity**: Major  
**Priority**: High  
**Связанный TC**: TC-XXXX

### Фактическое поведение
При выборе автора и нажатии «Применить»:
- Список MR фильтруется верно
- Но URL не обновляется (не добавляется `author=user2`)
- Кнопка «Сброс» не отображает активный фильтр

### Ожидаемое поведение
← Ссылка на TC-XXXX

### Шаги воспроизведения
1. Открыть репозиторий → «Запросы на слияние»
2. Открыть вкладку «Открыто»
3. Нажать «Фильтры» → выбрать автора
4. Нажать «Применить»

**Ожидаемый URL**: `/repos/owner/repo/pulls?state=open&author=user2`  
**Фактический URL**: `/repos/owner/repo/pulls?state=open`

### Окружение
- Браузер: Chrome 125.0.6422.78
- ОС: macOS 14.5
- Версия: web2 (Новый FE)
- Date: YYYY-MM-DD

### Скриншоты
- ![URL не обновляется](path/to/screenshot.png)
- ![Кнопка сброс не активна](path/to/screenshot2.png)

### Консольные ошибки
```
GET /api/v2/... 200 OK
Warning: URL parameter 'author' not synced
```

---

## Паттерны UI тестирования

### Элементы интерфейса

| Элемент | Что проверять |
|---------|---------------|
| Кнопка | Доступна? Активна? Подсветка при hover? |
| Поле ввода | Валидация? Placeholder? Ошибки? |
| Выпадающий список | Открывается? Выбор работает? |
| Табы | Переключение? Активный таб подсвечивается? |
| Модальное окно | Закрывается? Кнопки работают? |
| Заглушка | Появляется при пустом списке? Дизайн верный? |
| Лоадер | Исчезает после загрузки? Блокирует клики? |

### Состояния

| Состояние | Что проверять |
|-----------|---------------|
| Пустой список | Заглушка, кнопка создания, дизайн |
| Загрузка | Лоадер, блокировка, прогресс |
| Ошибка | Сообщение, кнопка повтора, логи |
| Успех | Toast, иконка, переход |
| Состояние загрузки | Прогресс-бар, остаток времени |

### Навигация

| Путь | Что проверять |
|------|---------------|
| Меню | Открывается/закрывается, пункты |
| Хлебные крошки | Рабочие ссылки, путь верный |
| URL | Обновляется, bookmarks работают |
| Back/Forward | Состояние сохраняется |

---

## Принципы UI тестирования

- **Координата обязательна.** Каждый TC привязан к узлу test-model (path + lv + теги)
- **Версия UI обязательна.** web1 и web2 — отдельные тесты
- **Шаги воспроизводимы.** Каждый шаг должен быть понятен другому тестировщику
- **Ожидаемый результат конкретен.** Не «работает», а «отображается 5 элементов»
- **Альтернативные сценарии обязательны.** Пустой список, ошибки, edge cases
- **Скриншоты важны.** Баг без скриншота — сложнее воспроизвести
- **UI-элементы проверять отдельно.** Не только функционал, но и UI-контролы

---

## Чего избегать

- ❌ Не писать TC без координаты узла (node + lv)
- ❌ Не тестировать только happy path — обязательны ошибки и пустые состояния
- ❌ Не смешивать web1 и web2 в одном TC
- ❌ Не описывать баг без скриншота
- ❌ Не игнорировать состояния лоадера (ожидание не завершается)
- ❌ Не тестировать deprecated-элементы без согласования
- ❌ Не писать «кликнуть на кнопку» — писать «нажать кнопку [Создать] в правом верхнем углу»

---

## Интеграция с test-model

```
SA: spec.md (AC-456) → UI: Фильтрация MR по автору
         ↓
QA: TC-XXXX с node: UI / ... / Фильтры / По автору (lv8)
         ↓
FE: web2 API + React-компоненты
```

Каждый UI-узел (lv6–lv8) должен быть покрыт TC. `#ПАО`-узлы — приоритет.

---

## Специфика web2 (Новый FE)

### Особенности тестирования

| Особенность | Как тестировать |
|-------------|-----------------|
| SPA-навигация | URL обновляется без перезагрузки |
| React-состояния | Лоадеры, error boundaries, suspense |
| GraphQL API | Запросы в Network tab (GraphQL), не REST |
| TypeScript | Ошибки в консоли при рендеринге |

### Проверка GraphQL запросов (web2)

```graphql
# В Chrome DevTools → Network → filter: graphql
query {
  repository(owner: "owner", name: "repo") {
    pullRequests(state: OPEN, first: 20) {
      nodes {
        id, title, author { login }
      }
    }
  }
}
```

**Что проверять:**
- Запрос приходит корректный
- Ответ содержит все поля
- Ошибки возвращаются в `errors` массиве
- Pagination работает через `pageInfo { hasNextPage, endCursor }`
